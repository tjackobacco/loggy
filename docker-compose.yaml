  services:
    api:
      build: .
      ports:
        - "8080:8080"
      environment:
        ConnectionStrings__Loggy: "Data Source=/data/loggy.db"
      volumes:
        - loggy-data:/data

  volumes:
    loggy-data: